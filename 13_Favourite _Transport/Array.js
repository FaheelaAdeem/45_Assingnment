var favourite_transport = ['car', 'byke', 'bus', 'train'];
var statement = "i would like to own a ";
for (var i = 0; i < favourite_transport.length; i++) {
    console.log(statement + favourite_transport[i]);
}
